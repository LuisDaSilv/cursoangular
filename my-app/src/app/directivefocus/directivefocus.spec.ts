import { Directivefocus } from './directivefocus';

describe('Directivefocus', () => {
  it('should create an instance', () => {
    const directive = new Directivefocus();
    expect(directive).toBeTruthy();
  });
});
